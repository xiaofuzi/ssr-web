var router = require('express').Router(),
	path = require('path');

// const users = [
//   { name: 'Alexandre' },
//   { name: 'Sébastien' }
// ]
/* GET users listing. */
//var jsonFiles = require('./mdToJson.js');
//let posts = jsonFiles(path.join(__dirname, '../_source'));

/*
router.get('/posts', function (req, res, next) {
    //res.json(posts);
})

router.get('/posts/:id', function (req, res, next) {
  var id = parseInt(req.params.id)
  if (id >= 0 && id < posts.length) {
    res.json(posts[id])
  } else {
    res.sendStatus(404)
  }
})
*/
module.exports = router
